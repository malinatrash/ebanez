version: '3.8'

services:
  bot:
    build: .
    volumes:
      - ./data:/app/data  # Persist the data directory
    env_file:
      - .env
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
